apiVersion: v1
kind: Service
metadata:
  name: main-service
  namespace: gym-jinni
  labels:
    app: main-service
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    name: grpc
  - port: 8081
    targetPort: 8081
    name: http
  selector:
    app: main-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: main-service
  namespace: gym-jinni
  labels:
    app: main-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: main-service
  template:
    metadata:
      labels:
        app: main-service
    spec:
      initContainers:
      - name: wait-for-postgres
        image: busybox:1.35
        command: ['sh', '-c', 'until nc -z postgres 5432; do echo waiting for postgres; sleep 2; done;']
      - name: run-migrations
        image: gym-jinni/service:latest
        command: ["/bin/sh", "-c"]
        args:
          - |
            apt-get update && apt-get install -y curl
            curl -L https://github.com/golang-migrate/migrate/releases/download/v4.15.2/migrate.linux-amd64.tar.gz | tar xvz
            mv migrate /usr/local/bin/
            cd /app/service
            migrate -path mig -database "${DBURL}" up || true
        env:
        - name: DBURL
          valueFrom:
            configMapKeyRef:
              name: gym-jinni-config
              key: DBURL
      containers:
      - name: main-service
        image: gym-jinni/service:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8080
          name: grpc
        - containerPort: 8081
          name: http
        env:
        - name: DBURL
          valueFrom:
            configMapKeyRef:
              name: gym-jinni-config
              key: DBURL
        livenessProbe:
          httpGet:
            path: /v1/users
            port: 8081
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /v1/users
            port: 8081
          initialDelaySeconds: 10
          periodSeconds: 5
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"

